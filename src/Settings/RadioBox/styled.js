import styled from "styled-components";

export const Input = styled.input`
  accent-color: rgb(0, 140, 150);
  scale: 1.3;
  margin-right: 10px;
  cursor: pointer;

  &:focus-visible {
    outline: 1px solid;
  }
`;